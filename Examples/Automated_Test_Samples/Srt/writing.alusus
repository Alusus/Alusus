import "common.alusus";

def Main: module
{
  def start: function () => Void
  {
    Srt.print("Hello World!!\n");

    def str: ptr[array[Char]];
    str = "abcdefgh";
    def i: Int;
    for i = 0, i < 8, ++i {
      Srt.putChar(str~cnt(i));
    };
    Srt.putChar('\n');
  };
};

run Main.start;

import "libalusus_spp.so";

def Main: module
{
  def EmptyType: type
  {
  };

  def MyType: type
  {
    def x: Int[32];
    def y: Int[32];
  };

  def WrongType: type;

  def start: function ()=>Void
  {
    def v: MyType;
    def e: EmptyType;

    print("x, y = %d, %d", v.x, v.y)
  }
};

dump_ast Main;

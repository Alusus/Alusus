 اشمل "عامة.أسس"؛

عرف ـرئيسي: حزمة
{
  عرف إبدأ: دالة ()
  {
    عرف صوان: مؤشر[ـصحيح]؛
    صوان = ـمتم.احجز(ـصحيح~حجم * 10)~مثل[مؤشر[ـصحيح]]؛

    عرف س: ـصحيح؛
    لكل س = 0، س < 10، ++س صوان~مثل[مؤشر[مصفوفة[ـصحيح]]]~محتوى(س) = س * س؛

    لكل س = 0، س < 10، ++س {
      ـمتم.اطبع("العنصر %d: %d\ج"، س، صوان~مثل[مؤشر[مصفوفة[ـصحيح]]]~محتوى(س))؛
    }؛

    ـمتم.حرر(صوان)؛
  }؛
}؛

نفذ ـرئيسي.إبدأ؛


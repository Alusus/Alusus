 اشمل "عامة.أسس"؛

عرف ـرئيسي: حزمة
{
  عرف إبدأ: دالة ()
  {
    عرف صوان: مؤشر[ـصحيح[32]]؛
    صوان = ـمتم.احجز(ـصحيح[32]~حجم * 10)~مثل[مؤشر[ـصحيح[32]]]؛

    عرف س: ـصحيح[32]؛
    لكل س = 0، س < 10، ++س صوان~مثل[مؤشر[مصفوفة[ـصحيح[32]، 0]]]~محتوى(س) = س * س؛

    لكل س = 0، س < 10، ++س {
      ـمتم.اطبع("العنصر %d: %d\ج"، س، صوان~مثل[مؤشر[مصفوفة[ـصحيح[32]، 0]]]~محتوى(س))؛
    }؛

    ـمتم.حرر(صوان)؛
  }؛
}؛

نفذ ـرئيسي.إبدأ؛


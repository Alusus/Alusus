import "common.alusus";

def UserInput: module {
  def start: function() {
    def eof: Int;
    eof = -1;

    def c : Int;
    c = Srt.getChar();
    while c != eof {
      Srt.putChar(c);
      c = Srt.getChar();
    };
  }
};

run UserInput.start;

import "Srl/Console.alusus";

module FactorialRecursive
{
  function factorial (i:Int) : Int
  {
    if i<=0 return 1;
    return i*factorial(i-1);
  };

  function start
  {
    Srl.Console.print("Factorial of 5 is %d\n", factorial(5));
  }
};

FactorialRecursive.start();


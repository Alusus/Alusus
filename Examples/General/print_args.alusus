import "Srl/Console.alusus";

def PrintArgs: module
{
  def start: function (argCount: Int, argv: ptr[array[ptr[Char]]]) => Int
  {
    def i: Int;
    for i = 0, i < argCount, ++i {
      Srl.Console.print("%s\n", argv~cnt(i));
    };
    return 0;
  };
};

run PrintArgs.start;
